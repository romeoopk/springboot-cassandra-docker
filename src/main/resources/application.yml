server:
  servlet:
    context-path: /bookstore/${application.api.version}
  port: 9999

application:
  api:
    version: v1
spring:
  cassandra:
    port: 9042
    contact-points: 127.0.0.1
    keyspace_name: keyspace_bookstore
    schema-action: CREATE_IF_NOT_EXISTS
    password: cassandra
    username: cassandra